public class FixDebugOne {
    /* This program displays a greeting */
    public static void main(String[] args) {
        System.out.println("Hello World!");
    }
}
    /*
     List down the spotted Bugs below:
     1. Invalid class name with space — instead of `FixDebug One` change to `FixDebugOne`
     2. Incorrect method name casing — instead of `Main`change to `main`
     3. Incorrect parameter declaration — instead of `String args` change to `String[] args`
     4. Typo in `System` — instead of `Systm` change to `System`
     5. Missing semicolon — added `;` after `System.out.println("Hello World!")`
     6. Missing closing curly braces.
     */